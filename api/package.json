{
  "name": "hub-controller-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute hub-controller-db --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute hub-controller-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.53.0"
  }
}
